Fixed an issue that prevented X following imports on iOS 18 when reading timestamps returned by the import service. Diagnostics for invalid import responses now include the failed stream stage and safe decoding context.
